<Window x:Class="KenticoLogViewer.Views.ConnectionManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage connections"
        Width="700" Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: connection list + action buttons -->
        <Grid Grid.Column="0" Margin="0,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListBox Grid.Row="0"
                     ItemsSource="{Binding Connections}"
                     SelectedItem="{Binding SelectedConnection}"
                     DisplayMemberPath="Name"
                     BorderThickness="1"
                     BorderBrush="#BDBDBD"/>

            <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,8,0,0">
                <Button Content="Add" Command="{Binding StartAddCommand}"
                        Margin="0,0,0,4" Padding="6,4"/>
                <Button Content="Edit" Command="{Binding StartEditCommand}"
                        Margin="0,0,0,4" Padding="6,4"/>
                <Button Content="Delete" Click="OnDeleteClick"
                        Padding="6,4"/>
            </StackPanel>
        </Grid>

        <!-- Right: edit form -->
        <Grid Grid.Column="1">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditing}" Value="False">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="Opacity" Value="0.4"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Nom *" FontWeight="SemiBold" Margin="0,0,0,2"/>
            <TextBox Grid.Row="1" Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10"/>

            <TextBlock Grid.Row="2" Text="Connection string *" FontWeight="SemiBold" Margin="0,0,0,2"/>
            <TextBox Grid.Row="3" Text="{Binding EditConnectionString, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap" Height="80" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>

            <UniformGrid Grid.Row="4" Columns="3" Margin="0,0,0,10">
                <StackPanel Margin="0,0,6,0">
                    <TextBlock Text="Table name" FontWeight="SemiBold" Margin="0,0,0,2"/>
                    <TextBox Text="{Binding EditTableName, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Margin="0,0,6,0">
                    <TextBlock Text="Max rows" FontWeight="SemiBold" Margin="0,0,0,2"/>
                    <TextBox Text="{Binding EditMaxRows, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Timeout (s)" FontWeight="SemiBold" Margin="0,0,0,2"/>
                    <TextBox Text="{Binding EditCommandTimeout, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </UniformGrid>

            <!-- Test connection -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,6">
                <Button Content="Test connection" Command="{Binding TestConnectionCommand}"
                        Padding="10,4" Margin="0,0,8,0"/>
                <ProgressBar IsIndeterminate="True" Width="60" Height="14" VerticalAlignment="Center">
                    <ProgressBar.Style>
                        <Style TargetType="ProgressBar">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsTestingConnection}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ProgressBar.Style>
                </ProgressBar>
            </StackPanel>
            <TextBlock Grid.Row="6" Text="{Binding TestResultMessage}" TextWrapping="Wrap"
                       Margin="0,0,0,8" VerticalAlignment="Top">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding TestResultMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Command="{Binding CancelEditCommand}"
                        Padding="14,6" Margin="0,0,8,0" IsCancel="True"/>
                <Button Content="Save" Command="{Binding SaveEditCommand}"
                        Padding="14,6" IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
